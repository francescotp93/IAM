# 15 settembre 2026 — Menu «Nuovo preventivo» rifatto, e cosa si è scoperto sull'impianto

> Voce da aggiungere **in fondo** a `raw/withus_memoria_strategica` nel vault
> `secondo-cervello`. Scritta qui perché la sessione girava in cloud e da lì il
> server MCP di Obsidian non risponde: il vault sta sul portatile.

---

## Che cosa è stato fatto

### Il menu «Nuovo preventivo» (brief del 14/09/2026)

**In produzione dal 15/09/2026**, fuso da Francesco via Claude con squash:
[QUOTE#154](https://github.com/francescotp93/QUOTE/pull/154) prima,
[Agente-sospesi#49](https://github.com/francescotp93/Agente-sospesi/pull/49) dopo.
Francesco ha confermato dal telefono: «Il menu su IAM funziona».

- Quattro categorie: Motor · Persona · Casa e Patrimonio · Impresa e Cauzioni.
  L'albero è `MEGA` in `withus-one.js`, annidato fino a quattro livelli
  (Impresa › RC Professionali › Tecnici › Geometri).
- Ogni foglia manda a QUOTO una chiave `prod`, risolta da `PRODOTTI_DIRETTI`
  in `QUOTE/index.html` (57 chiavi, prima 7). L'elenco è **contratto**:
  `INTERFACCIA-QUOTO-IAM.md` §2.6, identico nei due repository, sorvegliato da
  `verifica/menu-preventivo-albero.test.mjs` di qua e
  `server/verifica/prodotti-diretti.test.mjs` di là.
- Il menu si apre al clic e si chiude **solo** con clic fuori o Esc. Tolto il
  `mouseleave` che lo chiudeva muovendo il mouse e gli `:hover` che aprivano i
  flyout a colonna compressa. Il fuoco che passa nell'iframe di QUOTO vale come
  clic fuori: quei clic non arrivano al documento di IAM.
- Su telefono (≤900px) l'albero è una schermata intera, un livello alla volta,
  con «Indietro».
- «In arrivo» (RC Attività, Cyber): grigie, non link, non focusabili.
- Fotovoltaico: la città dell'impianto usa `resComuneSearch`, l'autocompletamento
  comuni del Motor.

### Decisioni prese sulle ambiguità del brief, e perché

| Decisione | Perché |
|---|---|
| TCM solo dentro Vita, non anche al primo livello | stesso prodotto due volte nella stessa colonna |
| Infortuni e Viaggio restano in Persona | il brief non li eliminava |
| Struttura (Sanitario) = le 4 strutture AMTRUST | in QUOTO non esiste un prodotto «Struttura» unico |
| Area Fiscale con tutte e 5 le sottocategorie | sono quelle della tariffa; il brief ne citava una |
| Rischi catastrofali abitazione aggiunta in Casa e Patrimonio | viveva sotto «Beni», che dal menu è sparita |
| Moduli eliminati dal menu **non** cancellati da QUOTO | decisione di prodotto; lo storico preventivi li usa |
| Sanitario › Medici = tariffa a classi `MEDICI` di QUOTO | «Medico Protetto» AMTRUST resta nel suo ramo |
| Tre foglie di Fideiussioni senza «Cauzione» nel nome | cercando «cauzione» deve uscire Cauzioni appalti |

Prodotti AMTRUST **esclusi**: Infortunih24, Infortuni Supervalore, Tutela al
Volante, Tutela Legale Medici&Sanitari, Tutela Medici. QUOTO li filtra fuori
(`amtProducts` mostra solo `rc_professionale`): non hanno schermata.

---

## Cose scoperte sull'impianto, che non stavano scritte da nessuna parte

- **IAM in produzione è GitHub Pages, non Vercel.** `iam.withusassicurazioni.it`
  risolve su GitHub (CNAME); Vercel fa solo le anteprime delle PR. Il VPS OVH
  ha già `/opt/withus-iam` aggiornato ogni minuto da `deploy/autopull.sh` di
  QUOTE (dal 21/08/2026), ma Caddy non ha ancora il blocco `iam.` e il DNS non
  è mai stato spostato. Piano scritto in `deploy/TRASLOCO-OVH.md` (QUOTE).
- **`quoto.withusassicurazioni.it` è GitHub Pages.** Solo `api.` è sul VPS.
- **Il proxy git delle sessioni web risponde 403 alla cancellazione di rami**
  (`git push origin --delete`). I rami fusi li cancella Francesco dal tasto
  «Delete branch» della PR. Da valutare: Settings → «Automatically delete head
  branches» nei due repository.
- **`iam.withusassicurazioni.it` risponde 403 alle richieste automatiche** dal
  contenitore di sessione: non si può verificare da lì il deploy di IAM.
  `quoto.` invece risponde.
- **La scocca si collauda in Chromium senza Supabase**: basta una pagina con
  `#app`, i bottoni `nb-*` che le voci rispecchiano (`data-mirror`), un
  `.panels` con un `.panel.act`, e `window.goTab = function(){}`. Playwright di
  QUOTE (`node_modules` dopo `npm i --no-save playwright@1.55`) fa il resto.
  Usato per 14 prove desktop e telefono, non committato perché IAM non ha
  Playwright fra le dipendenze.
- **`pkill -f <nome>` uccide anche la shell che lo lancia** se il nome compare
  nella riga di comando: esce con 144 e sembra un guasto. Non lo è.
- **Le PR si fondono con squash** in entrambi i repository.

---

## Decisione strategica in corso: un solo indirizzo, un solo repository

Francesco ha chiesto se unificare QUOTE e Agente-sospesi in un repository
**IAM** e portare IAM sul VPS al posto di Vercel. Risposta: sì, e il piano
esiste già a metà (`INDIRIZZO-UNICO.md` qui, `deploy/DOMINIO-UNICO.md` e
`deploy/TRASLOCO-OVH.md` in QUOTE, `UNIFICAZIONE.md`). Piano proposto:

1. **Un solo indirizzo, tutto sul VPS**: blocco Caddy versionato in QUOTE
   (`deploy/caddy/` + `deploy/setup.d/`), applicato dall'autopull; `iam.` dalla
   radice di `/opt/withus-iam`, `iam./nuovo-preventivo/` da `/opt/withus-backend`,
   i 23 percorsi di servizio al backend. Poi Francesco sposta il DNS su Aruba
   (`iam → A 51.254.142.199`). Solo dopo: `QUOTO` in `withus-one.js` e
   `QUOTO_URL` in `index.html` diventano `/nuovo-preventivo/`.
2. **Un solo repository**: Agente-sospesi entra in QUOTE come `iam/` con la sua
   storia; il repository si rinomina IAM; un solo contratto, un solo banco di
   prove; Agente-sospesi in archivio; Vercel spento; `CNAME` di Pages tolti per
   ultimi.
3. Via l'iframe e le tre schermate doppie (performance, utenti, storico), a
   moduli.

**Stato: in attesa del «vai» di Francesco per la prima PR (pacchetto Caddy).**

---

## Rimasti aperti

- RC Attività e Cyber restano «In arrivo» finché in QUOTO `IMPRESA_PRODUCTS`
  le tiene `active:false`. Quando arrivano le tariffe: attivarle in QUOTO, dare
  la chiave `prod` nel menu, riga in `INTERFACCIA-QUOTO-IAM.md` §2.6.
- Difetto cosmetico noto sotto dominio unico: una riga dei loghi compagnia in
  QUOTE usa `location.origin` (vedi `INDIRIZZO-UNICO.md`).
- I rami `claude/*` fusi il 15/09 sono stati cancellati da Francesco a mano.

---

## Aggiornamento 16 settembre 2026 — dominio unico acceso

- **QUOTE#156** fuso alle 08:18 UTC: sito Caddy `iam.withusassicurazioni.it`
  versionato in `deploy/caddy/iam.caddy`, impianto una-tantum via
  `setup.d/20-dominio-unico-caddy.sh` (riuscito al primo colpo, con backup
  `Caddyfile.buona-20260916-081823`), regola nell'autopull per gli
  aggiornamenti futuri, prova `deploy/dominio-unico.test.mjs`.
- **DNS su Aruba** (pannello `dns-panel.aruba.it`): Francesco ha prima
  dichiarato «fatto» ma il record non era salvato (il CNAME `iam` era ancora
  nella lista, nessun record A). Cancellato il CNAME e aggiunto
  `A iam → 51.254.142.199` alle ~09:50; i server autoritativi lo pubblicavano
  alle 09:59. **Lezione:** verificare sempre sugli autoritativi
  (`dig @dns.technorail.com`), non sul risolutore del VPS che tiene la cache
  per tutto il TTL (3600). Let's Encrypt guarda gli autoritativi: il reload
  di Caddy si puo' fare appena loro rispondono.
- **Cutover** alle 10:04: certificato ottenuto in 15 s dopo `systemctl
  reload caddy`; IAM 200, `/nuovo-preventivo/` 200 con motori e premi,
  sorgente 404, `api.` e `quoto.` invariati.
- **Agente-sospesi#51** (Diario: ore serali) e **#52** (riquadro su
  `/nuovo-preventivo/`, `QUOTO_ORIGIN` = origine di IAM) fusi. Vercel resta
  solo per le anteprime.
- **Da fare:** aggiungere `https://iam.withusassicurazioni.it/nuovo-preventivo/`
  ai Redirect URLs di Supabase Auth (reset password dal riquadro). Passo 2 del
  piano (repository unico «IAM») ancora da avviare. Il `CNAME` di GitHub Pages
  in Agente-sospesi si toglie quando si e' sicuri di non tornare indietro.

---

## Aggiornamento 16 settembre 2026, pomeriggio — repository unico (passo 2)

- **QUOTE#157** fuso (`main` 8123865): Agente-sospesi `main@e5b84ac` importato
  in `iam/` con `git read-tree --prefix=iam/`, un commit che cita l'origine.
  Alla radice restano **una sola copia** di `INTERFACCIA-QUOTO-IAM.md`,
  `IAM.md`, `WORKFLOW.md`; tolti da `iam/` anche `CNAME` e `.claude/`
  (`iam-specialist` ora in `.claude/agents/`). CLAUDE.md §10 racconta il
  cambio; `ui-test.mjs` cerca la scocca prima in `./iam`.
- **VPS**: Caddy serve `iam.` da `/opt/withus-backend/iam` (stesso autopull
  del backend); il blocco del secondo clone `/opt/withus-iam` e' uscito
  dall'autopull e dalla configurazione in esecuzione. Verificato dal canale
  comandi: HEAD 8123865, `index.html` di IAM servito con md5 identico a
  `main`, `/nuovo-preventivo/iam/` risponde 404.
- **Agente-sospesi#53** fuso (`aff88ef`): README e CLAUDE.md aprono con
  l'avviso di archivio e rimandano a QUOTE/`iam/`. Il repository e' ora
  **solo storia**: non si apre piu' nessuna PR li'.
- **Restano a Francesco, su GitHub e Vercel:** archiviare Agente-sospesi
  (Settings -> General -> Archive this repository); rimuovere il progetto
  Vercel `agente-sospesi`; rinominare QUOTE in **IAM** (Settings -> General ->
  Repository name); cancellare i rami `claude/*` fusi; aggiungere
  `https://iam.withusassicurazioni.it/nuovo-preventivo/` ai Redirect URLs di
  Supabase Auth.
- **Dopo la rinomina, PR da fare:** riferimenti al nuovo nome in
  `deploy/cmd-runner.sh` (REPO_PATH), `deploy/bootstrap.sh`,
  `server/deploy/setup.sh` e nei documenti; uno script `setup.d/` che
  aggiorna i remoti sul VPS (GitHub rimanda dal vecchio nome, quindi non c'e'
  fretta ma non va lasciato cosi'); togliere `/opt/withus-iam` dal disco.
- **Passo 3 (a moduli, piu' avanti):** via l'iframe, via le tre schermate
  doppie (performance, utenti, storico).

---

## Aggiornamento 16 settembre 2026, sera — il repository si chiama IAM

- Francesco ha archiviato `Agente-sospesi`, rimosso il progetto Vercel e
  rinominato `QUOTE` in **`francescotp93/IAM`** su GitHub.
- **IAM#159** fuso (`main` ff26950): cmd-runner, bootstrap e setup.sh clonano
  il nome nuovo; documenti vivi e agenti aggiornati; 9 prove nuove in
  `deploy/impianto.test.mjs` (21/21).
- **Sul VPS** lo script `setup.d/30-rinomina-repo-iam.sh` e' passato al primo
  giro (13:53 UTC): origin di `/opt/withus-backend` e `/opt/withus-cmd` su IAM,
  `/opt/withus-iam` tolto dal disco, Caddy non lo nominava. `iam.`,
  `/nuovo-preventivo/` e `api.` rispondono 200.
- **Lezione d'ambiente:** la sessione web ha lo scope GitHub col nome vecchio
  (`francescotp93/quote`); con il nome nuovo gli strumenti rispondono «Access
  denied», con il vecchio funzionano e GitHub rimanda. Lo stesso per il proxy
  git: `origin` deve restare sull'URL vecchio (col nuovo risponde 403, «not in
  this session's authorized repository set»); GitHub avvisa «This repository
  moved» e accetta. Nelle sessioni nuove lo scope avra' il nome giusto.
- **Aperto:** aggiungere `https://iam.withusassicurazioni.it/nuovo-preventivo/`
  ai Redirect URLs di Supabase Auth; cancellare i rami `claude/*` fusi; il
  `CNAME` di Pages resta finche' `quoto.` serve come strada di rientro.

---

## Aggiornamento 17 settembre 2026 — passo 3, prime due tessere

- **Sopralluogo del passo 3** (via l'iframe e le schermate doppie), fatto sul
  codice: «storico» **non** era una doppia (IAM = movimenti contabilita',
  `sessioni_giornaliere`; QUOTO = produzione, `quote_preventivi`); le doppie
  vere erano Utenti (QUOTO 15 KB su `iam_utenti`) e Performance (QUOTO 4 KB,
  irraggiungibile da IAM). Fondere i due documenti costa **44 nomi globali in
  comune** (`db`, `ME`, `initDB`, `onLogin`, `renderUtenti`, ...) e 37 `id`
  uguali. Utenti attivi: 4, di cui 1 solo-QUOTO (operativo): la strada «salto
  a pagina intera» con i token nell'hash serve a una persona.
- **Tessera 1, IAM#161**: `frame-ancestors 'self'` + `X-Frame-Options
  SAMEORIGIN` nell'header Caddy di `/nuovo-preventivo/`. Verificato servito.
- **Tessera 2, IAM#163** (decisione di Francesco: «collaboratori li gestisce
  IAM»): via la gestione utenti di QUOTO; `iam_utenti` la scrive solo IAM,
  QUOTO tocca solo `rete`/`responsabile`; `caricaProfilo` di QUOTO non crea
  piu' schede con `accesso_quoto: true` al primo accesso (era una porta
  aperta a chiunque avesse un account Auth). Nuovo messaggio `quoto-apri`
  (elenco chiuso nella scocca). Mappa «chi possiede quale schermata» in
  INTERFACCIA §2.7. Effetto collaterale utile: l'elenco operatori per
  «assegna a» ora si carica all'accesso, prima usciva vuoto a chi non apriva
  Utenti. 356/356 in browser, 6 prove nuove con controprova.
- **Brief di Francesco del 17/09** trascritto in `iam/docs/brief/`: Lavoro 1
  analisi previdenziale (cliente da anagrafica, ramo datoriale TFR, PDF come
  il preventivo personalizzato, invio email/WhatsApp con firma), Lavoro 2
  collaboratori e utenti in IAM (Operativa → Collaboratori, fonte unica
  `quote_collaboratori`, Utenti come albero con permessi a tre gruppi).
  Un merge alla volta. Il Lavoro 2 chiude anche il modulo 2b (spegnere la
  scheda collaboratori di QUOTO quando IAM avra' struttura, documenti, firma).
- **Aperti**: modulo 3 (grafico performance di QUOTO: tenere o togliere),
  modulo 4 (sessione senza passaggio token: stessa origine, stesso archivio),
  modulo 5 (il salto a pagina intera per l'unico solo-QUOTO).

---

## Aggiornamento 17 settembre 2026 — Lavoro 1, analisi previdenziale, in quattro PR

- **IAM#167, cliente dall'anagrafica**: componente unico «scegli un cliente»
  (`clp*`, sul modello di `resComuneSearch`), censimento in tendina con CF
  controllato; `Pensione.daAnagrafica`; foglio, archivio e server pretendono
  `anagrafica_id`. Le 7 analisi vecchie senza anagrafica restano.
- **IAM#169, ramo datoriale**: motore `tfr-datore.js`, 7 parametri di legge
  in `quote_parametri_previdenziali` con fonte (D.Lgs. 252/2005 art. 10,
  art. 2120 c.c., L. 296/2006 cc. 755 e 764, TUIR art. 77). Caso a mano:
  10 dipendenti a 2.000 € × 13 → 1.525 € il primo anno. **Da 50 addetti il
  confronto e' con la Tesoreria e per l'azienda e' neutro**: il motore lo
  dice. «TFR in azienda» si chiede al dipendente; con «no» il TFR non si
  nomina mai.
- **IAM#170, PDF**: `pdf-withus.js`, le primitive del personalizzato
  condivise; `Pensione.documentoPdf`; il personalizzato le usa per primo.
- **PR 4, invio**: `Pensione.testiInvio`, 12 varianti; firmatario dai
  collaboratori; email `/mail/send` con allegato; WhatsApp file/link firmato
  (bucket `preventivi`, `<anagrafica_id>/analisi-previdenziale.pdf`).
- **Lezione d'ambiente**: una PR che tocca tutti gli `start-service.sh` degli
  scraper (#168) tiene occupato l'autopull per ~5 minuti di riavvii: il giro
  successivo arriva dopo. Non e' un guasto, e la verifica dal canale comandi
  va rimandata di un giro.
- **Aperto**: il `numero` sul PDF della pensione e' la data (non c'e' un
  progressivo delle analisi); il tasto «Torna a IAM»/redirect Supabase; i
  moduli 3-5 del passo 3; il Lavoro 2 (collaboratori e utenti in IAM).

---

## Aggiornamento 17 settembre 2026 — Lavoro 2, collaboratori e utenti in IAM

- **IAM#172, PR 1, il registro unico**: `quote_collaboratori` e' la persona,
  `iam_team` l'allegato economico (`collab_id`), l'account `iam_id`.
  Migrazione con aggancio per CF poi email, **mai quando la corrispondenza
  non e' una**: due schede di `iam_team` con la stessa email restano due
  persone (id `1780477740576` e `1780477939669`, decide Francesco).
  «Operativa» → «Collaboratori». Blindatura di `iam_utenti` applicata:
  un utente non si cambia da solo ruolo, accessi, permessi, profilo, prodotti,
  moduli, caselle. 17 persone, 12 economie agganciate, 5 account agganciati.
- **PR 2, Utenti ad albero**: lista dalle persone (`righeUtenti`), RUI sulla
  riga, stato ATTIVO/INATTIVO/SOSPESO, ingranaggio con i tre gruppi
  (sezioni IAM · compagnie su Quoto · attivazione IAM). Colonna
  `iam_utenti.compagnie` (`null` = tutte, nomi dal catalogo prodotti) sotto la
  blindatura. Collegamento «imposta la password» con `resetPasswordForEmail`.
  Il vecchio «Nuovo utente» era rimasto senza nessuno che lo chiamasse: ora lo
  apre il gruppo 3 per una persona senza account, precompilato, e l'account
  nato scrive `iam_id` sulla persona. 13 prove nuove, controprova 11 rosse
  sul file di prima. Banco IAM 54/54.
- **Decisioni di Francesco**: link «imposta password», niente password in
  chiaro; blindatura applicata subito.
- **IAM#173, PR 2** fusa e viva sul VPS (md5 identico fra repo e Caddy).
- **PR 3, l'attivazione dal server**: `server/utenti.js`, `POST /utenti/attiva`
  (admin soltanto, riletto su `iam_utenti`); utenza con password casuale che
  nessuno vede, riga `iam_utenti`, `iam_id` sulla persona, collegamento
  recovery via `generate_link` verso `iam.withusassicurazioni.it/`, mail Brevo
  «Scegli la password ed entra». 409 se la persona ha gia' un account o
  l'email e' di un altro account. In IAM il gruppo 3 ha «Attiva l'accesso e
  manda il collegamento» con il ruolo; via `apriNuovoUtente`/`creaNuovoUtente`
  e la password temporanea nell'alert. In QUOTO `currentUser.compagnie` e
  `awCompagniaConsentita` nel confronto Motor. 7 + 4 prove nuove, 2 prove IAM
  adattate.
- **Trovato sui dati**: `quote_prodotti_catalogo.compagnie` dice «HD» su sette
  prodotti (auto, moto, autocarro, TCM, casa, infortuni, attivita'): e' HDI.
  Il filtro non spegne un nome che il catalogo non conosce, quindi HDI
  restava visibile a tutti. Francesco ha chiesto di correggerlo lo stesso
  giorno: `array_replace(compagnie, 'HD', 'HDI')`, 7 righe, nessun doppione
  (`20260917_catalogo_hd_hdi.sql`). Da qui HDI e' governata dal filtro.
- **Da fare a mano (Francesco)**: nelle impostazioni Auth di Supabase la
  Redirect URL `https://iam.withusassicurazioni.it/` deve essere fra quelle
  ammesse, altrimenti il collegamento della mail torna al Site URL; il
  «password dimenticata» di IAM usa gia' quell'origine, quindi dovrebbe
  esserci. Verificare col primo collaboratore attivato.
